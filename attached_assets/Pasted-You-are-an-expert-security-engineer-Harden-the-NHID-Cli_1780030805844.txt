You are an expert security engineer. Harden the NHID-Clinical audit trace system with cryptographic tamper-evidence and true append-only protection.

Current stack: FastAPI (gateway.py / saas layer), Postgres, existing hash-chained traces.

Do this carefully and defensively:

1. Database changes (audit_traces table):
   - Add these columns if missing (idempotent migration):
     - hmac_signature TEXT NOT NULL
     - policy_version TEXT
     - model_version TEXT
     - org_id TEXT NOT NULL
     - event_id UUID DEFAULT gen_random_uuid() PRIMARY KEY
   - Create a BEFORE INSERT OR UPDATE OR DELETE trigger that raises exception on any UPDATE or DELETE.
     Only INSERT allowed. Make it bulletproof.

2. Python side (create or update AuditService in gateway.py or a new audit.py):
   - Load HMAC_SECRET from Replit Secrets (fallback error if missing)
   - Before every trace insert:
     - Compute canonical payload (JSON sorted keys, no extra whitespace)
     - event_hash = SHA256(payload + prev_hash)
     - hmac_signature = HMAC-SHA256(HMAC_SECRET, event_hash + org_id + timestamp + policy_version)
     - Use constant-time comparison for any verification
   - Route ALL trace writes through this service (no direct DB inserts from anywhere else)
   - Support per-org or global chain (prefer per-org for isolation)

3. New endpoints (add to gateway.py):
   - GET /saas/audit/verify/{session_id} → full chain + HMAC validation, returns breaks if any
   - GET /saas/audit/proof/{session_id} → enhanced version that includes hmac_signature and verification status

4. Migration:
   - Safe backfill script for existing traces (compute hashes/HMACs without breaking old data)

5. Security:
   - Never log the HMAC secret
   - Reject any trace write without valid org_id
   - Add rate limiting on verify endpoint

After implementing:
- Show me the full table schema + trigger SQL
- Show the key Python functions (compute_event_hash, sign_event, verify_chain)
- Confirm the append-only trigger is active
- Restart the services and test one trace + verify roundtrip

Focus ONLY on the audit/trace layer. Do not touch billing, auth, or frontend.
Start by showing schema + trigger, ask for confirmation before writing code.